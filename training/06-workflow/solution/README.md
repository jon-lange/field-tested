# Solution

```bash
python3 diagnose.py
```

Every figure is computed from the lab. The reasoning, and what to do about each
finding, is in the module [README](../README.md).

## The shape all three share

Each produced output **indistinguishable from a healthy system** — no error, no
warning, nothing to notice. That is why review does not catch them.

> Given a green result, ask what a broken system would have printed. If the
> answer is "the same thing," the result is not evidence.
