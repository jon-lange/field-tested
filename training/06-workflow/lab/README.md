# Lab

```bash
python3 run.py
```

Read what it prints, then ask the question in the module
[README](../README.md) before opening the source. The answer is in the source,
which is why it is worth trying first.

## Rules

- Standard library only. No network, no keys, no model.
- Deterministic — two runs produce identical output.
- Do not change the inputs, thresholds, or stages. Everything here is
  reasonable; the problem is what is not being reported.

## When you are done

```bash
python3 ../solution/diagnose.py
```

Every figure it prints is computed from this lab.
